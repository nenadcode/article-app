import loginApi from '../../../api/login'

const state = {
  userEmail: '',
  userPassword: ''
}

const getters = {
  userEmail: state => state.userEmail,
  userPassword: state => state.userPassword
}

const actions = {

}